# 한남랭 (HannamLang)

한글 감탄사/의성어만으로 구성된 난해한 프로그래밍 언어.
엄랭(umjunsik-lang)에서 영감을 받아 제작.

## 키워드

| 키워드 | 의미 |
|--------|------|
| `예? 저요?` | 프로그램 시작 |
| `ㅋㅋ..엄` | 루프 시작 |
| `내아내임` | 프로그램 종료 + 반환값 |
| `엄...` | 변수 대입 (온점 최소 3개 필수) |
| `줴줴이야` | 문자(유니코드) 출력 |
| `요이` | 줄 점프 |
| `제가요?` | 정수 입력 |
| `티~원` | 개행 출력 |

## 숫자 표현

- `.` = +1
- `,` = −1
- `~` = 물결 카운트 (갯수 = 값)
- 공백 = 곱셈

```
...        => 3
,,         => -2
.,.,       => 0
티~~~~원   => 4 (물결 4개)
.. ..      => 4 (2 × 2)
... ...    => 9 (3 × 3)
```

## 변수

`ㅋ`를 반복하여 변수 인덱스 지정. 기본값 0.

| 표기 | 변수 |
|------|------|
| `ㅋ` | var[1] |
| `ㅋㅋ` | var[2] |
| `ㅋㅋㅋ` | var[3] |

## 연산

### 대입 (엄...)

`엄` 뒤에 온점 최소 3개 필수. 온점 3개 = 3 이상의 값만 대입 가능.

```
엄...             → var[1] = 3
엄.....           → var[1] = 5
ㅋ엄...           → var[2] = 3
ㅋㅋ엄ㅋ...       → var[3] = var[1] + 3
```

### 출력 (줴줴이야)

```
줴줴이야..         → '2' 출력 (정수)
줴줴이야아         → var[1] 값 출력
줴줴이야.......... ........  → 'X' 출력 (유니코드 88)
```

### 입력 — `제가요?`

`ㅋ` 접두사로 변수 지정.

```
제가요?           → var[1] = stdin 정수
ㅋ제가요?         → var[2] = stdin 정수
ㅋㅋ제가요?       → var[3] = stdin 정수
```

### 개행 (티~원)

```
티~원             → '\n' 출력
```

### 조건 — `아뇨{값}?{명령}`

값==0이면 명령 실행, 아니면 건너뜀.

```
아뇨아?줴줴이야..   → var[1]==0이면 2 출력
```

### 루프 (ㅋㅋ..엄 ... 아뇨아뇨아뇨)

`ㅋㅋ..엄`부터 `아뇨아뇨아뇨`까지 반복. `아뇨아뇨아뇨` 뒤의 값이 0이면 계속, 0이 아니면 탈출.

```
ㅋㅋ..엄
줴줴이야아
아아아엄아아아,
아뇨아뇨아뇨아아아
```
→ var[3]이 0이 될 때까지 반복 (do-while)

### 줄 점프 — `요이`

`요이` 뒤의 값번째 줄로 이동.

```
요이..            → 2번째 줄로 이동
요이.....         → 5번째 줄로 이동
```

### 조기종료 (내아내임)

`내아내임` + 반환값. 프로그램 즉시 종료.

```
내아내임.         → return 1
내아내임아        → return var[1]
```

## 프로그램 구조

```
예? 저요?
[명령어들]
내아내임.
```

- `예? 저요?`로 시작, `내아내임`으로 종료 (반환값 필수)
- 한 줄에 하나의 명령
- `#`으로 시작하는 줄은 주석
- `~`로 한줄 작성 가능 (줄바꿈 대체)

## 파일 확장자

`.한남` (또는 `.hnr`)
